{% extends 'base.html.twig' %}

{% block title %}Hello PalierController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Scénario : {{ scenario.nom }}</h1>
    {% for choix in scenario.getIdChoix %}
    <h2> <a href="{{path('app_consequence', {'id': choix.id })}}">{{ choix.nom}}<h2>
    <p>{{choix.description}}<p>
    {% endfor %}
</div>
        {{include("palier/_footer.html.twig")}}
{% endblock %}
