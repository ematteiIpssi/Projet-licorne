{% extends 'base.html.twig' %}

{% block body %}
    <style>
        body {
            background: url('{{ asset('assets/images/win.webp') }}') no-repeat center center fixed;
            background-size: cover;
        }
    </style>

    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <h1 style="font-size: 50px;">Bravo !</h1>
        <a href="{{ path('app_licorne_index')}}" class="btn btn-warning">Retour au scoreboard</a>
    </div>
{% endblock %}
