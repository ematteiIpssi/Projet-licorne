{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {# Votre contenu de page ici #}
    </div>

    <footer class="fixed-bottom bg-light">
        <div class="container">
            <div class="example-wrapper">
             <p>{{ licorne.nom }} | PV : {{licorne.pv}} | Force : {{licorne.strenght}} | Esquive : {{licorne.esquive}}<p/>
            </div>
        </div>
    </footer>
{% endblock %}
